# Agentic Scientific Discovery — dependencies
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
requests>=2.28
python-dotenv>=1.0
statsmodels>=0.14

# GEO dataset loading
GEOparse>=2.0

# LLM backends (install the one you need)
openai>=1.0           # For OpenAI or gateway backends
# ollama               # For Ollama backend (optional, uses requests)

# Embeddings & retrieval
sentence-transformers>=3.0

# HuggingFace model support (for local LLMs and fine-tuning)
transformers>=4.36
huggingface-hub>=0.20
datasets>=2.16
accelerate>=0.25

# GPU: PyTorch is required for ML tools, neural networks, and GPU-accelerated DE
# Install separately for your CUDA version:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Or CPU-only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
